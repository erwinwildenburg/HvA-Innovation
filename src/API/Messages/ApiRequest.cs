using System;

namespace API.Messages
{
    /// <summary>
    /// A class that all API requests are extended on.
    /// This makes sure everything in here will be included in every request.
    /// </summary>
    public class ApiRequest { }
}
